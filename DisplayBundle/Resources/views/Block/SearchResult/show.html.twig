{##
 # This file is part of the PHPOrchestra\DisplayBundle.
 #
 # @author Benjamin Fouché <benjamin.fouche@businessdecision.com>
 ##}
{#{ dump(resultset.numfound) }#}

{# if they are 0 result #}
{% if resultset.numfound is not defined %}
    {% if resultset is empty %}
        {{ 'php_orchestra_cms.search_result.view.no_result_found'|trans }} "{{ data }}".
    {% else %}
        {# Browse the result list and call this template #}
        {% for result in resultset %}
            {% include "PHPOrchestraDisplayBundle:Block/SearchResult:show.html.twig" with {"data": data, "resultset": result, "nodeId": nodeId} %}
        {% endfor %}
    {% endif %}
{% else %}
    <div id="searchid">
    {% if resultset.spellcheck is not empty %}
        {# Call spellCheck template #}
        {% include "PHPOrchestraDisplayBundle:Block/SearchResult:spellcheck.html.twig" %}
    {% endif %}
    
    {# This template display list of facet #}
    {% if resultset.facetSet is not empty %}
        {% include "PHPOrchestraDisplayBundle:Block/SearchResult:facet.html.twig" %}
    {% endif %}
    
    {% if resultset is not empty %}
        {# This template display list of documents in the result#}
        {% include "PHPOrchestraDisplayBundle:Block/SearchResult:showDocs.html.twig" %}
    {% else %}
        {{ 'php_orchestra_cms.search_result.view.no_result_found'|trans }} "{{ data }}".
    {% endif %}
    
    </div>
{% endif %}
